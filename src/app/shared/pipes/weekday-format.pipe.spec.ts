import { WeekdayFormatPipe } from './weekday-format.pipe';

describe('WeekdayFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekdayFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
