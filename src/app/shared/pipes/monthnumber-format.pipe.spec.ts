import { MonthNumberFormatPipe } from './monthnumber-format.pipe';

describe('MonthNumberFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthNumberFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
